plugins {
    id 'java'
    id 'org.cyclonedx.bom' version '1.9.0'
}

group = "org.poornesh"
version = "1.0.0"

repositories {
    mavenCentral()
}

dependencies {
    implementation 'com.google.guava:guava:32.1.2-jre'
    implementation 'org.apache.commons:commons-lang3:3.14.0'
    implementation 'ch.qos.logback:logback-classic:1.4.14'

    testImplementation 'junit:junit:4.13.2'
    testImplementation 'org.assertj:assertj-core:3.25.3'
}

tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}

// CycloneDX SBOM configuration for Gradle 9+
import org.cyclonedx.gradle.CycloneDxPluginExtension

extensions.configure(CycloneDxPluginExtension) { ext ->
    ext.includeConfigs = ['implementation', 'runtimeClasspath']
    ext.skipConfigs = ['testImplementation', 'testRuntimeClasspath']
    ext.projectType = 'library'
    ext.componentGroup = project.group
    ext.componentName = project.name
    ext.componentVersion = project.version.toString()
    ext.outputFormat = 'json'
    ext.outputLocation = file("$buildDir/sbom/gradle-sbom.json")
    ext.includeBuildSystem = true
    ext.includeLicenseText = true
    ext.includeMetadataResolution = true
    ext.organizationalEntity = org.cyclonedx.model.OrganizationalEntity().with {
        name = "PoorneshORG"
        urls = ["https://github.com/PoorneshORG"]
        it
    }
}
